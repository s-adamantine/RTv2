render {
		{name, {Teeny Triangle}}
		{dimensions, {100 100}}
		{ambient, {4}}
}

camera {
		{origin, {5.0 0.0 0.0}}
		{rotation, {0.0 0.0 0.0}}
}
light {
		{origin, {10.0 10.0 10}}
		{color, {1.0 1.0 1.0}}
		{intensity, {10 10}}
}

object {
		{type, {mesh}}
		{path, {./scenes/objs/teapot.obj}}
		{reflection, {0.1 0 0. 10}}
		{color, {0.0 1.0 0.0}}
}
